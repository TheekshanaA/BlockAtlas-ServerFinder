pluginManagement {
    repositories {
        maven { url "https://maven.fabricmc.net/" }
        maven { url "https://maven.architectury.dev/" }
        maven { url "https://maven.minecraftforge.net/" }
        gradlePluginPortal()
    }
}

include("common")
include("fabric")
//include("forge") // disabled: Forge remapping fails with Loom 1.11 for this MC version
//include("neoforge") // disabled: Yarn mappings have unfixable conflicts with NeoForge remapping